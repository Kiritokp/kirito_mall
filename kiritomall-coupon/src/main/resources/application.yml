server:
  port: 7000

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.196.128:8848
  application:
    name: kiritomall-coupon
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

